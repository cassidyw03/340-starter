<!-- Content Heading -->
 <% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Message Block (when needed) -->
<% messages() %>

<!-- Inventory Items Display -->
 <%- grid %>

 